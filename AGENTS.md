# 운영 사이트 작업 규칙

기존 글, URL, 제휴 추적 정보와 사용자 작업을 보존한다. 비밀키를 코드·채팅·로그에 출력하지 않는다.

2026-09-13 사용자 승인 예외: 기존 주말 준비물 18개는 새 주제로 교체하며 URL 유지 의무가 없다. 교체 시 잘못된 기존 글을 공개 데이터·목록·사이트맵에서 삭제하고 새 글이 사용하지 않는 기존 이미지도 배포 산출물에서 제거한다. 다른 카테고리의 글과 URL은 보존한다. 실제 서버에서 이전 URL과 이미지 응답을 확인하기 전에는 서버 완전 삭제라고 보고하지 않는다.

## 쿠팡 API 절대규칙

어느 시점에서든 직전 60초 동안 같은 계정의 API 요청 횟수와 상품 조회량은 각각 10을 넘으면 안 된다. 이는 이 프로젝트의 운영 상한이며 쿠팡 공식 허용량이 아니다. 계정 공식 제한이 더 엄격하면 더 엄격한 값을 적용한다.

- 기본 요청은 상품 1개, 요청 시작 간격 최소 6.1초. 실패와 재시도도 모두 차감한다.
- 모든 관리자·자동·수동 작업은 `scripts/prep/coupang.mjs`의 `coupangFetch`만 사용한다. 직접 fetch, 로컬 시간표 파일, 메모리 전용 gate 사용 금지.
- 사용자가 승인한 저비용 방식: 모든 서버는 기존 GitHub 저장소의 `codex/coupang-shared-state` 공용 기록을 사용한다. 파일 SHA 비교 후 교체(CAS)로 요청 소유권을 획득하며 기록이나 인증이 없거나 불명확하면 호출하지 않는다. Redis 또는 로컬 제한기로 자동 우회하지 않는다. 기록 브랜치는 삭제·강제 재설정하지 않는다.
- 중앙 lock에는 TTL을 주지 않는다. 응답 상태가 불명확한 작업 또는 재시작 후 lock을 자동 삭제하지 않는다. 원래 작업이 완전히 종료됐는지 확인한 뒤 중앙 대기시간을 포함해 수동 복구한다.
- 상품 개수가 불명확한 goldbox/bestcategories는 호출 금지. 현재 adapter는 search limit=1만 허용한다.
- 캐시 재사용, 429 Retry-After 준수. 방문자 렌더링에서 상품 API 호출 금지.
- 같은 계정의 다른 프로그램이 이 경로를 우회하면 전역 한도를 보장할 수 없다. 연결 전 운영자가 모든 클라이언트 경유 여부를 확인한다.

## 주말 준비물

새 글은 `shoppable-scene-v2` 형식이다. 주제에 맞는 실제 상품과 사진·옵션을 먼저 확인한 다음 상품 3~4개를 한 생활 장면으로 합성한다. 사진마다 서로 다른 상품 3~4개의 + 태그를 연결하고 본문에 각각의 역할·선택 기준·주의점을 설명한다. 물·물티슈로 모든 주제를 채우지 않는다. 생성에 실제 사용한 사진 URL을 referenceProducts에 기록하고 합성 결과·태그 좌표를 대조한 뒤에만 productMatchReviewed를 승인한다. 상품 외형이 다른 이미지는 재사용하지 않는다. 기존 18개는 교체 전까지 구형 콘텐츠로 구분한다.

비공개 초안은 발행하지 않는다. 실제 등록된 상품과 원본 제휴 URL만 사용한다. 상품명·사진·규격·옵션을 대조하고 출처·확인시각을 기록한다. 개인 경험, 맛 평가, 별점, 미확인 가격/할인/배송일/조리시간/권장 연령/성능을 만들지 않는다.

주말 준비물 발행 글의 이미지는 대표 썸네일을 포함해 글당 최소 3장, 최대 4장이다. 기본은 썸네일 1장과 서로 다른 설명 장면 2장이다. 개수를 채우기 위한 같은 장면 반복은 금지한다. 대표 이미지는 감성적인 한글 문구를 사용한다. AI 연출 표시를 붙이고 실제 상품 재현이 불확실하면 '이 활동에 어울리는 준비물'로 연결한다. 이미지는 검토하고 `public/prep-images`에 압축 저장한다. 작업 중인 미완성 초안은 저장할 수 있지만 3장 미만은 발행하지 않는다.

최신 요청에 10회와 15회가 함께 등장하므로 더 엄격한 직전 60초 최대 10회·10개를 적용한다. 11번째부터 대기하고 15회로 늘리지 않는다. 기존 중앙 state가 더 낮은 cap이면 그대로 존중한다. 이는 자체 운영 상한이며 공식 허용량이 아니다.

글은 독자의 검색 의도에 맞는 고유 제목/H1, 준비 순서, 선택 기준, 대체 방법, 정리 팁을 포함한다. 주제마다 필요한 구성을 선택하고 동일 틀의 유사 글 양산을 금지한다. 제목·메타 설명·canonical·사이트맵·실제 내용과 일치하는 Article/Breadcrumb를 적용하고 검색 성과를 보장하지 않는다.

발행 검사는 글별로 실행한다. 한 글 실패로 다른 준비된 글을 막지 않는다. `docs/weekend-prep-operations.md`를 참고한다.
