// Local-only visual contact sheet; no data mutation or publication.
import http from 'node:http';import fs from 'node:fs';
const escape=s=>s.replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('"','&quot;');
http.createServer((req,res)=>{const page=Number(new URL(req.url,'http://localhost').searchParams.get('page')||0);const articles=JSON.parse(fs.readFileSync('.cache/waug/preview.json','utf8')).articles.slice(page*10,page*10+10);res.setHeader('Content-Type','text/html;charset=utf-8');res.end(`<html lang="ko"><head><title>공식 사진 장소 대조 ${page+1}</title><style>body{font-family:Arial;margin:15px;background:#fafafa}.grid{display:grid;grid-template-columns:repeat(5,1fr);gap:12px}figure{margin:0;background:white;border:1px solid #ddd;padding:5px}img{width:100%;height:220px;object-fit:contain}figcaption{font-size:15px;line-height:1.5}</style></head><body><h1>공식 사진 장소 대조 ${page+1}</h1><div class="grid">${articles.map(a=>`<figure><img src="${escape(a.thumbnail.url)}"><figcaption>${escape(a.slug)}<br>${escape(a.title)}</figcaption></figure>`).join('')}</div></body></html>`)}).listen(3011,'127.0.0.1',()=>console.log('Local image audit http://127.0.0.1:3011'));
